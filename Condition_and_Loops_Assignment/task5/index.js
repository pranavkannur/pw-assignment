let num = 1;

// Ascending order
while (num <= 25) {
    console.log(num);
    num++;
}

// Reset num for descending order
num = 25;

// Descending order
while (num >= 1) {
    console.log(num);
    num--;
}
